type TrackListProps = {
    tracks: any[];
}

const TrackList = ({ tracks }: TrackListProps) => {
    return (
        tracks.map((track, key) => (
            <></>
        ))
    )
};

export default TrackList;
